# Model arguments
model_name_or_path: meta-llama/Llama-2-7b-hf
model_revision: main
torch_dtype: bfloat16
attn_implementation: eager

# Data training arguments
# chat_template: "{% for message in messages %}{% if loop.first and messages[0]['role'] != 'system' %}{{ '<|im_start|>system\nYou are a helpful AI assistant named xLlama, trained by Patrick Haller<|im_end|>\n' }}{% endif %}{{'<|im_start|>' + message['role'] + '\n' + message['content'] + '<|im_end|>' + '\n'}}{% endfor %}{% if add_generation_prompt %}{{ '<|im_start|>assistant\n' }}{% endif %}"
chat_template: "### r/{{ subreddit }} Question:\n\n{{ instruction }}\n\n### r/{{ subreddit }} Answer:\n\n{{ output }}"
dataset_name: PatrickHaller/opinion_gpt_dataset
dataset_config: default
dataset_train_split: {bias}
dataset_test_split: test
dataset_num_proc: 24

# SFT trainer config
bf16: true
do_eval: false
eval_strategy: "no"
gradient_accumulation_steps: 1
gradient_checkpointing: true
gradient_checkpointing_kwargs:
  use_reentrant: false
hub_model_id: smollm2-1.7B-sft
hub_strategy: every_save
learning_rate: 3.0e-04 
log_level: info
logging_steps: 5
logging_strategy: steps
lr_scheduler_type: cosine
# max_seq_length: 2048
max_length: 2048
pad_to_multiple_of: 64
max_steps: -1
num_train_epochs: 3
output_dir: /vol/tmp/hallepat/opinion_gpt/adapters2/{bias}
overwrite_output_dir: true
per_device_eval_batch_size: 2
per_device_train_batch_size: 8
push_to_hub: false
remove_unused_columns: true
report_to:
- wandb
save_strategy: "epoch"
seed: 42
warmup_ratio: 0.1

use_peft: True
# target_modules: ["q_proj", "v_proj", "k_proj", "o_proj", "gate_proj", "up_proj", "down_proj"]
